framework:
    secret: secret

    form:
        enabled: true

    test: ~

    mailer:
        transports:
            dsn: 'null://null'

    translator: ~

    session:
        storage_id: session.storage.mock_file
        handler_id: session.handler.native_file

FOS_user:
    firewall_name:  main

    from_email:  'no-reply@localhost'

    user_class:  'FOS\UserBundle\Tests\App\Entity\User'

    group:
        group_class: 'FOS\UserBundle\Tests\App\Entity\Group'
